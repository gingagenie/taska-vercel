Taska — Xero‑style Quote/invoice Mockup
· typescript
# Taska Xero‑Style Quotes/Invoices — Replit‑Ready (Complete)
                </div>
                <div className="col-span-1 text-right">
                  <input type="number" step="0.01" value={it.discount} onChange={(e) => setItem(it.id, 'discount', Number(e.target.value))} className="w-full border rounded-lg px-3 py-2 text-right" />
                </div>
                <div className="col-span-1">
                  <select value={it.tax} onChange={(e) => setItem(it.id, 'tax', e.target.value)} className="w-full border rounded-lg px-3 py-2">
                    <option>GST</option>
                    <option>None</option>
                  </select>
                </div>
                <div className="col-span-1 text-right font-medium pt-2">{currency(lineTotal)}</div>
                <div className="col-span-12 flex items-center justify-between pt-2">
                  <button className="text-sm text-neutral-600 hover:text-neutral-900" onClick={addRow}>+ Add row</button>
                  {items.length > 1 && (
                    <button className="text-sm text-red-600" onClick={() => removeRow(it.id)}>Remove</button>
                  )}
                </div>
              </div>
            );
          })}
        </div>


        <div className="mt-6 grid grid-cols-12 gap-6">
          <div className="col-span-12 lg:col-span-7">
            <div className="bg-white rounded-2xl border shadow-sm p-4">
              <div className="text-sm font-medium text-neutral-700">Notes / Terms</div>
              <textarea className="mt-2 w-full border rounded-xl px-3 py-2 min-h-[96px]" placeholder="e.g. Payment due within 7 days. Parts remain property of FMF until paid in full."></textarea>
              <div className="mt-3 flex items-center gap-3 text-sm">
                <label className="flex items-center gap-2"><input type="checkbox" className="scale-110" /><span>Attach photos</span></label>
                <label className="flex items-center gap-2"><input type="checkbox" className="scale-110" /><span>Show deposit request (30%)</span></label>
              </div>
            </div>
          </div>


          <div className="col-span-12 lg:col-span-5">
            <div className="bg-white rounded-2xl border shadow-sm p-4">
              <div className="flex items-center justify-between text-sm"><span className="text-neutral-600">Subtotal</span><span className="font-medium">{currency(totals.subtotal)}</span></div>
              <div className="flex items-center justify-between text-sm mt-2"><span className="text-neutral-600">Total GST</span><span className="font-medium">{currency(totals.gst)}</span></div>
              <div className="h-px bg-neutral-200 my-3" />
              <div className="flex items-center justify-between text-lg font-semibold"><span>Total</span><span>{currency(totals.total)}</span></div>
              <div className="mt-4 flex gap-2">
                <button className="flex-1 px-3 py-2 rounded-xl bg-neutral-900 text-white text-sm shadow-sm disabled:opacity-50" disabled={!contact} onClick={sendDoc}>
                  {isQuote ? 'Send quote' : 'Send invoice'}
                </button>
                <button className="px-3 py-2 rounded-xl border text-sm" onClick={() => window.print()}>Print</button>
              </div>
            </div>
          </div>
        </div>


        <div className="mt-8 text-center text-xs text-neutral-500">Dev build. API at <code>/api</code>. Replace fake send with Stripe/Xero when ready.</div>
      </div>
    </div>
  );
}
```


---
## `.replit` (optional)
```ini
run = "npm run dev"
```


---
## `README.md`
```md
# Taska Quotes/Invoices — Replit Quickstart


**Dev**
1. `npm install`
2. `npm run dev`
3. Open the webview (Vite on 5173; API on 3000 via proxy)


**Prod**
1. `npm run build`
2. `npm start` — Express serves `dist/`


Where to integrate:
- Email/PDF/Stripe/Xero → `server/index.js` `/api/send`
- Presets from DB → replace in-memory `PRESETS`
```



You’re right—my bad. I’ve just replaced it on the canvas with a complete, Replit-ready project (no fragments):

Front end: Vite + React + Tailwind

API: tiny Express server (/api/presets, /api/send)

Dev proxy: 5173 → 3000

Prod: Express serves the built app